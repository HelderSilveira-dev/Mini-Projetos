// Este mini-projeto é a criação de uma função que usa dois números e retorna o maior.

let valorMaior = max(15,18);
console.log(valorMaior);

function max(numero1, numero2) {
    if(numero1 > numero2)
        return numero1;
    else return numero2;    
    
}
